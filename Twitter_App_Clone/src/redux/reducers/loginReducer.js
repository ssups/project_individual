const init = {
  userId: null,
  isLogined: false,
};

export default loginRedcuer = (state = init, { type, payload }) => {
  switch (type) {
    case value:
      break;

    default:
      return state;
  }
};
